import { t as supabase } from "./client-BZaQbCef.js";
import { n as useAuth } from "./auth-context-J0MGvtDS.js";
import { a as formatCurrency, i as INVESTMENT_TYPES, o as formatDate } from "./format-CUlL4Oov.js";
import { i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-BXjpJ96D.js";
import { n as Input, t as Label } from "./label-N2deYTEC.js";
import { t as Button } from "./button-PJVP9td7.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-C0WYWEQX.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";
import { Briefcase, Plus, Trash2, TrendingDown, TrendingUp } from "lucide-react";
import { z } from "zod";
//#region src/routes/_authenticated/investments.tsx?tsr-split=component
var schema = z.object({
	name: z.string().trim().min(1).max(80),
	asset_type: z.string().trim().min(1),
	invested_amount: z.number().nonnegative().max(0xe8d4a51000),
	current_value: z.number().nonnegative().max(0xe8d4a51000),
	purchased_on: z.string()
});
var COLORS = [
	"hsl(292 76% 50%)",
	"hsl(292 60% 70%)",
	"hsl(155 55% 55%)",
	"hsl(75 75% 60%)",
	"hsl(27 80% 60%)",
	"hsl(210 70% 55%)",
	"hsl(340 70% 60%)"
];
function InvestmentsPage() {
	const { user } = useAuth();
	const uid = user.id;
	const qc = useQueryClient();
	const q = useQuery({
		queryKey: ["investments", uid],
		queryFn: async () => (await supabase.from("investments").select("*").eq("user_id", uid).order("purchased_on", { ascending: false })).data ?? []
	});
	const add = useMutation({
		mutationFn: async (v) => {
			const { error } = await supabase.from("investments").insert({
				...v,
				user_id: uid
			});
			if (error) throw error;
		},
		onSuccess: () => {
			toast.success("Investment added");
			qc.invalidateQueries({ queryKey: ["investments", uid] });
			qc.invalidateQueries({ queryKey: ["dashboard", uid] });
		},
		onError: (e) => toast.error(e.message)
	});
	const del = useMutation({
		mutationFn: async (id) => {
			const { error } = await supabase.from("investments").delete().eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => {
			toast.success("Removed");
			qc.invalidateQueries({ queryKey: ["investments", uid] });
			qc.invalidateQueries({ queryKey: ["dashboard", uid] });
		}
	});
	const rows = q.data ?? [];
	const invested = rows.reduce((s, r) => s + Number(r.invested_amount), 0);
	const currentVal = rows.reduce((s, r) => s + Number(r.current_value), 0);
	const gain = currentVal - invested;
	const gainPct = invested > 0 ? gain / invested * 100 : 0;
	const allocation = /* @__PURE__ */ new Map();
	rows.forEach((r) => allocation.set(r.asset_type, (allocation.get(r.asset_type) ?? 0) + Number(r.current_value)));
	const pieData = Array.from(allocation.entries()).map(([name, value]) => ({
		name,
		value
	}));
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-6 sm:space-y-8",
		children: [
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h1", {
				className: "font-display text-2xl sm:text-3xl font-bold",
				children: "Investment Portfolio"
			}), /* @__PURE__ */ jsx("p", {
				className: "mt-1 text-sm sm:text-base text-muted-foreground",
				children: "Track holdings, returns and asset allocation."
			})] }),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-3 sm:gap-4 grid-cols-2 lg:grid-cols-4",
				children: [
					/* @__PURE__ */ jsx(StatCard, {
						icon: /* @__PURE__ */ jsx(Briefcase, { className: "h-5 w-5" }),
						label: "Invested",
						value: formatCurrency(invested)
					}),
					/* @__PURE__ */ jsx(StatCard, {
						icon: /* @__PURE__ */ jsx(TrendingUp, { className: "h-5 w-5" }),
						label: "Current value",
						value: formatCurrency(currentVal),
						accent: "text-primary"
					}),
					/* @__PURE__ */ jsx(StatCard, {
						icon: gain >= 0 ? /* @__PURE__ */ jsx(TrendingUp, { className: "h-5 w-5" }) : /* @__PURE__ */ jsx(TrendingDown, { className: "h-5 w-5" }),
						label: "Total gain/loss",
						value: formatCurrency(gain),
						accent: gain >= 0 ? "text-success" : "text-destructive"
					}),
					/* @__PURE__ */ jsx(StatCard, {
						icon: /* @__PURE__ */ jsx(TrendingUp, { className: "h-5 w-5" }),
						label: "Return %",
						value: `${gainPct >= 0 ? "+" : ""}${gainPct.toFixed(2)}%`,
						accent: gainPct >= 0 ? "text-success" : "text-destructive"
					})
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-6 lg:grid-cols-3",
				children: [/* @__PURE__ */ jsxs(Card, {
					className: "lg:col-span-2 shadow-soft",
					children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, { children: "Add investment" }) }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx(InvestmentForm, {
						onSubmit: (v) => add.mutate(v),
						pending: add.isPending
					}) })]
				}), /* @__PURE__ */ jsxs(Card, {
					className: "shadow-soft",
					children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, { children: "Allocation" }) }), /* @__PURE__ */ jsx(CardContent, { children: pieData.length === 0 ? /* @__PURE__ */ jsx("p", {
						className: "text-sm text-muted-foreground",
						children: "Add investments to see allocation."
					}) : /* @__PURE__ */ jsx("div", {
						className: "h-56 w-full",
						children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(PieChart, { children: [/* @__PURE__ */ jsx(Pie, {
							data: pieData,
							dataKey: "value",
							innerRadius: 40,
							outerRadius: 80,
							paddingAngle: 2,
							children: pieData.map((_, i) => /* @__PURE__ */ jsx(Cell, { fill: COLORS[i % COLORS.length] }, i))
						}), /* @__PURE__ */ jsx(Tooltip, { formatter: (v) => formatCurrency(v) })] }) })
					}) })]
				})]
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "shadow-soft",
				children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, { children: "Holdings" }) }), /* @__PURE__ */ jsx(CardContent, {
					className: "p-0",
					children: rows.length === 0 ? /* @__PURE__ */ jsx("div", {
						className: "p-8 text-center text-sm text-muted-foreground",
						children: "No investments yet."
					}) : /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableHead, { children: "Name" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Type" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Purchased" }),
						/* @__PURE__ */ jsx(TableHead, {
							className: "text-right",
							children: "Invested"
						}),
						/* @__PURE__ */ jsx(TableHead, {
							className: "text-right",
							children: "Current"
						}),
						/* @__PURE__ */ jsx(TableHead, {
							className: "text-right",
							children: "P/L"
						}),
						/* @__PURE__ */ jsx(TableHead, {})
					] }) }), /* @__PURE__ */ jsx(TableBody, { children: rows.map((r) => {
						const pl = Number(r.current_value) - Number(r.invested_amount);
						const pct = Number(r.invested_amount) > 0 ? pl / Number(r.invested_amount) * 100 : 0;
						return /* @__PURE__ */ jsxs(TableRow, { children: [
							/* @__PURE__ */ jsx(TableCell, {
								className: "font-medium",
								children: r.name
							}),
							/* @__PURE__ */ jsx(TableCell, { children: r.asset_type }),
							/* @__PURE__ */ jsx(TableCell, { children: formatDate(r.purchased_on) }),
							/* @__PURE__ */ jsx(TableCell, {
								className: "text-right font-mono",
								children: formatCurrency(Number(r.invested_amount))
							}),
							/* @__PURE__ */ jsx(TableCell, {
								className: "text-right font-mono",
								children: formatCurrency(Number(r.current_value))
							}),
							/* @__PURE__ */ jsxs(TableCell, {
								className: `text-right font-mono ${pl >= 0 ? "text-success" : "text-destructive"}`,
								children: [
									pl >= 0 ? "+" : "",
									formatCurrency(pl),
									" (",
									pct.toFixed(1),
									"%)"
								]
							}),
							/* @__PURE__ */ jsx(TableCell, {
								className: "text-right",
								children: /* @__PURE__ */ jsx("button", {
									onClick: () => del.mutate(r.id),
									className: "rounded p-1.5 text-muted-foreground hover:bg-destructive/10 hover:text-destructive",
									children: /* @__PURE__ */ jsx(Trash2, { className: "h-4 w-4" })
								})
							})
						] }, r.id);
					}) })] })
				})]
			})
		]
	});
}
function StatCard({ icon, label, value, accent = "" }) {
	return /* @__PURE__ */ jsx(Card, {
		className: "shadow-soft",
		children: /* @__PURE__ */ jsxs(CardContent, {
			className: "p-4 sm:p-5",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-2 text-[11px] sm:text-xs uppercase tracking-wider text-muted-foreground",
				children: [/* @__PURE__ */ jsx("span", {
					className: "shrink-0",
					children: icon
				}), /* @__PURE__ */ jsx("span", {
					className: "truncate",
					children: label
				})]
			}), /* @__PURE__ */ jsx("div", {
				className: `mt-2 font-display text-lg sm:text-2xl font-bold break-words ${accent}`,
				children: value
			})]
		})
	});
}
function InvestmentForm({ onSubmit, pending }) {
	const [name, setName] = useState("");
	const [assetType, setAssetType] = useState(INVESTMENT_TYPES[0]);
	const [invested, setInvested] = useState("");
	const [current, setCurrent] = useState("");
	const [date, setDate] = useState(() => (/* @__PURE__ */ new Date()).toISOString().slice(0, 10));
	function submit(e) {
		e.preventDefault();
		const parsed = schema.safeParse({
			name,
			asset_type: assetType,
			invested_amount: Number(invested),
			current_value: Number(current || invested),
			purchased_on: date
		});
		if (!parsed.success) {
			toast.error(parsed.error.issues[0]?.message ?? "Invalid");
			return;
		}
		onSubmit(parsed.data);
		setName("");
		setInvested("");
		setCurrent("");
	}
	return /* @__PURE__ */ jsxs("form", {
		onSubmit: submit,
		className: "grid gap-4 grid-cols-1 sm:grid-cols-2 md:grid-cols-6",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "sm:col-span-2 md:col-span-2",
				children: [/* @__PURE__ */ jsx(Label, { children: "Name" }), /* @__PURE__ */ jsx(Input, {
					value: name,
					onChange: (e) => setName(e.target.value),
					placeholder: "e.g. Apple Stock",
					required: true,
					maxLength: 80
				})]
			}),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Type" }), /* @__PURE__ */ jsx("select", {
				value: assetType,
				onChange: (e) => setAssetType(e.target.value),
				className: "mt-1.5 h-10 w-full rounded-md border border-input bg-background px-3 text-sm",
				children: INVESTMENT_TYPES.map((t) => /* @__PURE__ */ jsx("option", { children: t }, t))
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Invested" }), /* @__PURE__ */ jsx(Input, {
				type: "number",
				min: "0",
				step: "0.01",
				value: invested,
				onChange: (e) => setInvested(e.target.value),
				placeholder: "0.00",
				required: true
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Current value" }), /* @__PURE__ */ jsx(Input, {
				type: "number",
				min: "0",
				step: "0.01",
				value: current,
				onChange: (e) => setCurrent(e.target.value),
				placeholder: "0.00"
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Purchased" }), /* @__PURE__ */ jsx(Input, {
				type: "date",
				value: date,
				onChange: (e) => setDate(e.target.value),
				required: true
			})] }),
			/* @__PURE__ */ jsx("div", {
				className: "sm:col-span-2 md:col-span-6",
				children: /* @__PURE__ */ jsxs(Button, {
					type: "submit",
					disabled: pending,
					className: "w-full sm:w-auto gradient-primary",
					children: [/* @__PURE__ */ jsx(Plus, { className: "mr-1 h-4 w-4" }), " Add investment"]
				})
			})
		]
	});
}
//#endregion
export { InvestmentsPage as component };
