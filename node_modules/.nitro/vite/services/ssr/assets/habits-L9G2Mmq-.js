import { t as supabase } from "./client-BZaQbCef.js";
import { n as useAuth } from "./auth-context-J0MGvtDS.js";
import { i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-BXjpJ96D.js";
import { n as Input, t as Label } from "./label-N2deYTEC.js";
import { t as Button } from "./button-PJVP9td7.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { format, subDays } from "date-fns";
import { Check, Flame, Plus, Trash2 } from "lucide-react";
//#region src/routes/_authenticated/habits.tsx?tsr-split=component
function HabitsPage() {
	const { user } = useAuth();
	const uid = user.id;
	const qc = useQueryClient();
	const habitsQ = useQuery({
		queryKey: ["habits", uid],
		queryFn: async () => (await supabase.from("habits").select("*").eq("user_id", uid).order("created_at", { ascending: false })).data ?? []
	});
	const logsQ = useQuery({
		queryKey: ["habit_logs", uid],
		queryFn: async () => (await supabase.from("habit_logs").select("*").eq("user_id", uid)).data ?? []
	});
	const [name, setName] = useState("");
	const [description, setDescription] = useState("");
	const [frequency, setFrequency] = useState("daily");
	const addHabit = useMutation({
		mutationFn: async () => {
			const trimmed = name.trim();
			if (!trimmed) throw new Error("Habit name is required");
			if (trimmed.length > 100) throw new Error("Name too long");
			const { error } = await supabase.from("habits").insert({
				user_id: uid,
				name: trimmed,
				description: description.trim() || null,
				frequency
			});
			if (error) throw error;
		},
		onSuccess: () => {
			toast.success("Habit added");
			setName("");
			setDescription("");
			qc.invalidateQueries({ queryKey: ["habits", uid] });
		},
		onError: (e) => toast.error(e.message)
	});
	const toggleLog = useMutation({
		mutationFn: async ({ habit_id, day, existingId }) => {
			if (existingId) {
				const { error } = await supabase.from("habit_logs").delete().eq("id", existingId);
				if (error) throw error;
			} else {
				const { error } = await supabase.from("habit_logs").insert({
					user_id: uid,
					habit_id,
					logged_on: day
				});
				if (error) throw error;
			}
		},
		onSuccess: () => {
			qc.invalidateQueries({ queryKey: ["habit_logs", uid] });
			qc.invalidateQueries({ queryKey: ["dashboard", uid] });
		},
		onError: (e) => toast.error(e.message)
	});
	const removeHabit = useMutation({
		mutationFn: async (id) => {
			const { error } = await supabase.from("habits").delete().eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => {
			toast.success("Habit removed");
			qc.invalidateQueries({ queryKey: ["habits", uid] });
			qc.invalidateQueries({ queryKey: ["habit_logs", uid] });
		}
	});
	const habits = habitsQ.data ?? [];
	const logs = logsQ.data ?? [];
	const today = format(/* @__PURE__ */ new Date(), "yyyy-MM-dd");
	const last14 = Array.from({ length: 14 }).map((_, i) => format(subDays(/* @__PURE__ */ new Date(), 13 - i), "yyyy-MM-dd"));
	function streakOf(habit_id) {
		const days = new Set(logs.filter((l) => l.habit_id === habit_id).map((l) => l.logged_on));
		let streak = 0;
		for (let i = 0; i < 365; i++) {
			const d = format(subDays(/* @__PURE__ */ new Date(), i), "yyyy-MM-dd");
			if (days.has(d)) streak++;
			else break;
		}
		return streak;
	}
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-8",
		children: [
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h1", {
				className: "font-display text-2xl sm:text-3xl font-bold",
				children: "Financial habits"
			}), /* @__PURE__ */ jsx("p", {
				className: "mt-1 text-muted-foreground",
				children: "Save daily. Track weekly. Invest monthly. Small acts compound."
			})] }),
			/* @__PURE__ */ jsxs(Card, {
				className: "shadow-soft",
				children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, { children: "New habit" }) }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsxs("form", {
					onSubmit: (e) => {
						e.preventDefault();
						addHabit.mutate();
					},
					className: "grid gap-4 md:grid-cols-4",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "md:col-span-2",
							children: [/* @__PURE__ */ jsx(Label, { children: "Name" }), /* @__PURE__ */ jsx(Input, {
								value: name,
								onChange: (e) => setName(e.target.value),
								placeholder: "Save $10 today",
								maxLength: 100,
								required: true
							})]
						}),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Frequency" }), /* @__PURE__ */ jsxs("select", {
							value: frequency,
							onChange: (e) => setFrequency(e.target.value),
							className: "mt-1.5 h-10 w-full rounded-md border border-input bg-background px-3 text-sm",
							children: [
								/* @__PURE__ */ jsx("option", {
									value: "daily",
									children: "Daily"
								}),
								/* @__PURE__ */ jsx("option", {
									value: "weekly",
									children: "Weekly"
								}),
								/* @__PURE__ */ jsx("option", {
									value: "monthly",
									children: "Monthly"
								})
							]
						})] }),
						/* @__PURE__ */ jsx("div", {
							className: "flex items-end",
							children: /* @__PURE__ */ jsxs(Button, {
								type: "submit",
								disabled: addHabit.isPending,
								className: "w-full gradient-primary",
								children: [/* @__PURE__ */ jsx(Plus, { className: "mr-1 h-4 w-4" }), " Add"]
							})
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "md:col-span-4",
							children: [/* @__PURE__ */ jsx(Label, { children: "Description (optional)" }), /* @__PURE__ */ jsx(Input, {
								value: description,
								onChange: (e) => setDescription(e.target.value),
								placeholder: "Why does this habit matter?",
								maxLength: 200
							})]
						})
					]
				}) })]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-4",
				children: [habits.length === 0 && /* @__PURE__ */ jsx(Card, {
					className: "shadow-soft",
					children: /* @__PURE__ */ jsxs(CardContent, {
						className: "p-8 text-center text-sm text-muted-foreground",
						children: [
							"Start with something small: ",
							/* @__PURE__ */ jsx("em", { children: "“Track today’s expenses”" }),
							" or ",
							/* @__PURE__ */ jsx("em", { children: "“Save $5”" }),
							"."
						]
					})
				}), habits.map((h) => {
					const habitLogs = logs.filter((l) => l.habit_id === h.id);
					const streak = streakOf(h.id);
					const todayLog = habitLogs.find((l) => l.logged_on === today);
					return /* @__PURE__ */ jsx(Card, {
						className: "shadow-soft",
						children: /* @__PURE__ */ jsxs(CardContent, {
							className: "p-4 sm:p-5",
							children: [
								/* @__PURE__ */ jsxs("div", {
									className: "flex flex-wrap items-start justify-between gap-3",
									children: [/* @__PURE__ */ jsxs("div", {
										className: "min-w-0 flex-1",
										children: [/* @__PURE__ */ jsxs("div", {
											className: "flex flex-wrap items-center gap-2",
											children: [/* @__PURE__ */ jsx("h3", {
												className: "font-display text-base sm:text-lg font-semibold break-words",
												children: h.name
											}), /* @__PURE__ */ jsx("span", {
												className: "rounded-full bg-secondary px-2 py-0.5 text-xs text-secondary-foreground",
												children: h.frequency
											})]
										}), h.description && /* @__PURE__ */ jsx("p", {
											className: "mt-1 text-sm text-muted-foreground break-words",
											children: h.description
										})]
									}), /* @__PURE__ */ jsxs("div", {
										className: "flex flex-wrap items-center gap-2",
										children: [
											/* @__PURE__ */ jsxs("div", {
												className: "flex items-center gap-1 rounded-md bg-primary/10 px-2.5 py-1 text-sm font-semibold text-primary",
												children: [
													/* @__PURE__ */ jsx(Flame, { className: "h-4 w-4" }),
													" ",
													streak
												]
											}),
											/* @__PURE__ */ jsxs(Button, {
												size: "sm",
												variant: todayLog ? "secondary" : "default",
												className: todayLog ? "" : "gradient-primary",
												onClick: () => toggleLog.mutate({
													habit_id: h.id,
													day: today,
													existingId: todayLog?.id
												}),
												children: [
													/* @__PURE__ */ jsx(Check, { className: "mr-1 h-4 w-4" }),
													" ",
													todayLog ? "Done" : "Mark done"
												]
											}),
											/* @__PURE__ */ jsx("button", {
												onClick: () => removeHabit.mutate(h.id),
												className: "rounded p-2 text-muted-foreground hover:bg-destructive/10 hover:text-destructive",
												children: /* @__PURE__ */ jsx(Trash2, { className: "h-4 w-4" })
											})
										]
									})]
								}),
								/* @__PURE__ */ jsx("div", {
									className: "mt-4 grid grid-cols-14 gap-1",
									style: { gridTemplateColumns: "repeat(14, minmax(0, 1fr))" },
									children: last14.map((d) => {
										return /* @__PURE__ */ jsx("div", {
											title: d,
											className: `aspect-square rounded ${habitLogs.some((l) => l.logged_on === d) ? "bg-primary" : "bg-muted"}`
										}, d);
									})
								}),
								/* @__PURE__ */ jsx("div", {
									className: "mt-1 text-xs text-muted-foreground",
									children: "Last 14 days"
								})
							]
						})
					}, h.id);
				})]
			})
		]
	});
}
//#endregion
export { HabitsPage as component };
