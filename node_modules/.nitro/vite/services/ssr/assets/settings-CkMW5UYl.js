import { t as supabase } from "./client-BZaQbCef.js";
import { n as useAuth } from "./auth-context-J0MGvtDS.js";
import { c as setAppCurrency, t as CURRENCIES } from "./format-CUlL4Oov.js";
import { i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-BXjpJ96D.js";
import { n as Input, t as Label } from "./label-N2deYTEC.js";
import { t as Button } from "./button-PJVP9td7.js";
import { useEffect, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Settings } from "lucide-react";
//#region src/routes/_authenticated/settings.tsx?tsr-split=component
function SettingsPage() {
	const { user } = useAuth();
	const uid = user.id;
	const qc = useQueryClient();
	const q = useQuery({
		queryKey: ["profile", uid],
		queryFn: async () => (await supabase.from("profiles").select("*").eq("id", uid).maybeSingle()).data
	});
	const [fullName, setFullName] = useState("");
	const [currency, setCurrency] = useState("USD");
	const [monthlyIncome, setMonthlyIncome] = useState("0");
	useEffect(() => {
		if (q.data) {
			setFullName(q.data.full_name ?? "");
			setCurrency(q.data.currency ?? "USD");
			setMonthlyIncome(String(q.data.monthly_income ?? 0));
			setAppCurrency(q.data.currency ?? "USD");
		}
	}, [q.data]);
	const save = useMutation({
		mutationFn: async () => {
			const { error } = await supabase.from("profiles").update({
				full_name: fullName,
				currency,
				monthly_income: Number(monthlyIncome) || 0
			}).eq("id", uid);
			if (error) throw error;
		},
		onSuccess: () => {
			setAppCurrency(currency);
			toast.success("Settings saved");
			qc.invalidateQueries({ queryKey: ["profile", uid] });
		},
		onError: (e) => toast.error(e.message)
	});
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-8",
		children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs("h1", {
			className: "font-display text-2xl sm:text-3xl font-bold flex items-center gap-2",
			children: [/* @__PURE__ */ jsx(Settings, { className: "h-7 w-7 text-primary" }), " Settings"]
		}), /* @__PURE__ */ jsx("p", {
			className: "mt-1 text-muted-foreground",
			children: "Personalize your WealthPulse experience."
		})] }), /* @__PURE__ */ jsxs(Card, {
			className: "shadow-soft max-w-2xl",
			children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, { children: "Profile & Preferences" }) }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsxs("form", {
				onSubmit: (e) => {
					e.preventDefault();
					save.mutate();
				},
				className: "space-y-5",
				children: [
					/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Email" }), /* @__PURE__ */ jsx(Input, {
						value: user?.email ?? "",
						disabled: true
					})] }),
					/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Full name" }), /* @__PURE__ */ jsx(Input, {
						value: fullName,
						onChange: (e) => setFullName(e.target.value),
						maxLength: 100
					})] }),
					/* @__PURE__ */ jsxs("div", { children: [
						/* @__PURE__ */ jsx(Label, { children: "Preferred currency" }),
						/* @__PURE__ */ jsx("select", {
							value: currency,
							onChange: (e) => setCurrency(e.target.value),
							className: "mt-1.5 h-10 w-full rounded-md border border-input bg-background px-3 text-sm",
							children: CURRENCIES.map((c) => /* @__PURE__ */ jsx("option", {
								value: c.code,
								children: c.label
							}, c.code))
						}),
						/* @__PURE__ */ jsx("p", {
							className: "mt-1 text-xs text-muted-foreground",
							children: "Used throughout the app for money formatting."
						})
					] }),
					/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Monthly income (optional benchmark)" }), /* @__PURE__ */ jsx(Input, {
						type: "number",
						min: "0",
						step: "0.01",
						value: monthlyIncome,
						onChange: (e) => setMonthlyIncome(e.target.value)
					})] }),
					/* @__PURE__ */ jsx(Button, {
						type: "submit",
						disabled: save.isPending,
						className: "gradient-primary",
						children: "Save changes"
					})
				]
			}) })]
		})]
	});
}
//#endregion
export { SettingsPage as component };
